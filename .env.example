# NestLancer Frontend Environment Variables
# NOTE: Only client-safe variables (prefixed with VITE_) are exposed to frontend
# NEVER include sensitive secrets in frontend - they belong in the backend only

# Backend API Configuration
VITE_API_BASE_URL=http://localhost:8000/api
VITE_WS_URL=ws://localhost:8000

# Third-party Integration (Public Keys Only)
VITE_RAZORPAY_KEY_ID=rzp_test_your_public_key_here
VITE_CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=your_upload_preset

# Analytics & Monitoring (Public Configuration)
VITE_GA_TRACKING_ID=G-XXXXXXXXXX

# Application Features
VITE_ENABLE_DEV_TOOLS=true
VITE_PWA_ENABLED=false
VITE_MAINTENANCE_MODE=false

# Internationalization
VITE_DEFAULT_LOCALE=en
VITE_FALLBACK_LOCALE=en

# Performance & Debugging
VITE_LOG_LEVEL=info
VITE_API_TIMEOUT=30000

# App Metadata
VITE_APP_NAME=NestLancer
VITE_APP_VERSION=1.0.0
VITE_APP_DESCRIPTION=Freelancing platform for project management and collaboration

# IMPORTANT SECURITY NOTES:
# - JWT secrets, Razorpay secret keys, database URLs, API keys must stay in BACKEND only
# - Only expose public configuration that the frontend UI needs
# - All business logic validation and processing happens in backend
# - Frontend is UI-only with no sensitive operations
